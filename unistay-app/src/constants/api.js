// Your machine's LAN IP — physical devices on the same WiFi use this.
// Update this if your IP changes (run `ipconfig` to find it).
const LAN_IP = '192.168.1.9';

export const API_URL = `http://${LAN_IP}:3000`;
